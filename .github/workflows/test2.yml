name: Practice Workflow to perform security scans on push

# on: 
#   workflow-dispatch:
#   pull-request:
#     branches: [ "main" ]
#   push:
#     branches: [ "main" ]

jobs:
  checkout-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      run: python main.py


